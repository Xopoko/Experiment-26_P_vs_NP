# Q43.S322 nk base ratio bound

We show `Q43_grid_ratio (n_k + 1) >= 2^(2k+1)/(2k+1)^5` for k>=1.
This uses the log2-jump equality at `n_k+1` and the bound `(n_k+1)^2 >= 2^(2k+1)`.
The lemma is `Q43_grid_ratio_nk_succ_lower` and it feeds the gap-right lift
via an explicit base bound in terms of k.

Lean: `formal/WIP/Verified/Q43.lean` (lemmas `Q43_grid_ratio_nk_succ_lower`,
`Q43_thm41_log2_threshold_c1_grid_pow5_scaled_simple_of_ratio_gap_right`).
