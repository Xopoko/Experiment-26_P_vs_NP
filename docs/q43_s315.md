# Q43.S315 HR log2 bound equivalence
Artifact: Reduction/Equivalence.
Lean: `formal/WIP/Verified/Q43.lean` (lemma `Q43_hrThreshold_log2_bound_iff_pow`).
Statement: HR log2 bound is equivalent to `256 * log2(|F|)^(2(c+1)) <= |F|`.
Dependencies: Nat.le_div_iff_mul_le, Nat.pow_le_pow_iff_left, Nat.mul_pow, Nat.pow_mul.
Usage: reduces the c-bound step to a single power inequality on |F|.
