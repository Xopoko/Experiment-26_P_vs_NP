# Q43.S334 gap-left cover
We add a gap-left lift for the scaled log2^5 threshold.
Key steps: log2(grid_size) is constant (=2k) on [2^k, n_k],
giving monotone grid_ratio on the left band, and a new base bound
at 2^k derived from k0+1.
These combine with the n_k+1 branch to cover n <= 3*2^(k-1).
Lean: `formal/WIP/Verified/Q43.lean`
