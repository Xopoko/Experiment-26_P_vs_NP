# Q43.S327 regime-d thread from gap-right n0

We thread the gap-right `n0(C)` threshold into the regime-d bundle by
composing the n0-based scaled log2^5 bound with the regime-d parameter
wrapper. This yields a single lemma that converts
`n0(C) <= n < 2^(k0(C)+1)` plus the log2 parameter inequality into
`Q43_thm41_regime_d_ok_param n N`.

Lean: `formal/WIP/Verified/Q43.lean` (lemma
`Q43_thm41_regime_d_ok_param_of_gap_right_n0`).
