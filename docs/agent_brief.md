# Agent brief (bounded working memory)

Правило: этот файл — «оперативная память» агента. Он **не должен разрастаться**
(лимит проверяется в `scripts/verify_notebook.py`). Обновляй его **заменой/сжатием**,
а не дописыванием бесконечных списков.

## Текущее направление (держать 1–2 строки)

Proof complexity → (условные) EF‑нижние оценки/формализация (Pich–Santhanam 2023) и связанные барьеры.

## Что уже сделано (не повторять)

- ROABP‑канонизация multilinearization для CNF даёт $\mathrm{P}=\mathrm{NP}$ (барьер‑леммы 15.7.4* в `docs/15_proof_complexity.md`).
- PIT‑аксиомы ⇒ EF p‑симулирует IPS (каркас + ссылки в `docs/15_proof_complexity.md`).
- Базовые «формализационные кирпичики» (CNF→3‑CNF equisat + явный $g$, Tseitin‑Eval, счётчики/пороги) уже вынесены в `docs/research/16_research_steps.md`.

## Активные “неповторимые” задачи (выбрать одну за прогон)

- Q1a/Q2a/Q3a/Q4a из `docs/open_questions.md` (обновлять там же, но без раздувания).

## Реестр экспериментов (макс 12 пунктов; перезаписывать старые)

- E01: Формализация PIT‑аксиом (GP14 Def. 1.7) и связь с SoundnessIPS → EF sim IPS (см. 15.7.3).
- E02: Барьер канонизации $\mathrm{ML}(P_\varphi)$ → ROABP (см. 15.7.4*).
- E03: LogLog‑трюк для bounded‑кванторов/таблиц истинности (toy‑шаги в 16.x).
- E04: Контрпример «эквивалентность CNF→3‑CNF» и корректный режим equisatisfiable + явный $g$ (15.7.3c–d).
- E05: Tseitin‑кодирование $\mathrm{Eval}(C,x)$ даёт 3‑CNF $O(s)$ (toy‑шаги 16.x).
- E06: Счётчик ошибок (adder‑tree/Batcher) и компаратор порога не меняют $\mathrm{poly}(2^n)$ (toy‑шаги 16.x).
- E07: Конкретизация NP‑полных подклассов для 15.7.4b (planar 3‑SAT; bounded‑occurrence 3‑SAT).
- E08: —
- E09: —
- E10: —
- E11: —
- E12: —

## Линзы (держать 5 последних; обновлять, не наращивать)

Последние: Эквивалентность → Сжатие/канонизация → Инвариант → Трейд‑офф → (пусто)
