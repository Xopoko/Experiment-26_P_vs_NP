# Q43.S329 apply gap-right to flat-eval chain

We apply the gap-right regime-d wrapper directly in the quasi-poly
flat-eval chain. The new lemma packages the regime-d parameter check
and the lineMax t-parameter bound under `n0(C) <= n < 2^(k0(C)+1)` with
`C = (log2 |F|)^c`, and then feeds that into the flat-eval statement.

Lean: `formal/WIP/Verified/Q43.lean` (lemmas
`Q43_quasipoly_regime_d_ok_param_lineMax_of_gap_right`,
`Q43_flat_eval_statement_of_quasipoly_gap_right`).
