Сверься с docs/agent_brief.md, последним коммитом, сводкой python3 agent/analyze_logs.py и хвостом agent/logs/latest_trio.log (или agent/logs/latest_duo.log / agent/logs/latest.log) чтобы понять, что делал WORKER/SKEPTIC, проверь scripts/verify_all.sh, затем как Supervisor (без новой математики) оцени последний WORKER‑шаг на валидность по протоколу (ровно 1 артефакт типа Proof/Counterexample/Exact citation/Toy/Reduction/Barrier + StepID/InfoGain + нет повтора Do-not-repeat + есть commit + Lean-first соблюдён: содержательные заметки в formal/PvNP/*.lean или formal/Notes/*.lean, а docs/research/*.md не трогали), и при необходимости обнови только docs/open_questions.md и docs/agent_brief.md (BLOCKED/NextStepID/Do-not-repeat), а если по логам виден системный цикл/сбой — минимально поправь scripts/agent_prompt.txt / scripts/skeptic_prompt.txt / scripts/supervisor_prompt.txt / AGENTS.md / agent/*.sh (только чтобы убрать повтор/сбой), и если были правки — сделай осмысленный git commit.
