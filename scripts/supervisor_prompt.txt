Сверься с docs/agent_brief.md, последним коммитом, сводкой python3 agent/analyze_logs.py и хвостом agent/logs/latest_trio.log (или agent/logs/latest_duo.log / agent/logs/latest.log) чтобы понять, что делал WORKER/SKEPTIC, проверь python3 scripts/verify_notebook.py, затем как Supervisor (без новой математики) оцени последний WORKER‑шаг на валидность (ровно 1 артефакт типа Proof/Counterexample/Exact citation/Toy + StepID/InfoGain + нет повтора Do-not-repeat, и если SKEPTIC отметил проблему — она отражена), и при необходимости обнови только docs/open_questions.md и docs/agent_brief.md (BLOCKED/NextStepID/Do-not-repeat), а если по логам виден системный цикл/сбой — минимально поправь scripts/agent_prompt.txt / scripts/skeptic_prompt.txt / scripts/supervisor_prompt.txt / AGENTS.md / agent/*.sh (только чтобы убрать повтор/сбой), и если были правки — сделай осмысленный git commit.
