[$web-search]($CODEX_HOME/skills/web-search/SKILL.md) Read AGENTS.md. Write run contract via scripts/write_run_contract.py (uses $CONTRACT_FILE); run python3 scripts/stopper_advice.py --contract "$CONTRACT_FILE" --mode pre and if it exits 42 then immediately produce a BLOCKED/Barrier artifact with NextStepID and stop, else continue; write run meta via scripts/write_run_meta.py (uses $RUN_META_FILE) after verify. Start the next run.
