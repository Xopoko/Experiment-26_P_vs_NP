Используй приложенный ниже “Question Set” от QUESTION_SETTER как единственный план (если он пуст — выбери один активный пункт сам), выполни выбранный микрошаг и оформи ровно 1 проверяемый артефакт типа Proof/Counterexample/Exact citation/Toy/Reduction/Barrier (с барьер‑чеком, StepID и InfoGain), оформи результат Lean-first (по возможности формализуй в formal/PvNP/*.lean или formal/External/*.lean, а длинные заметки держи в formal/Notes/*.lean; docs/research/*.md не редактируй, это редиректы), затем минимально обнови docs/open_questions.md и docs/agent_brief.md, прогони scripts/verify_all.sh и сделай один осмысленный git commit.
